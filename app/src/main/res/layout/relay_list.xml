<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorBackgroundDashboard">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:gravity="center">
        <TextView
            android:id="@+id/textViNameRelay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="10dp"
            android:background="@drawable/shape_title"
            android:textColor="@color/colorTextTitle"
            android:text="@string/RelayText"
            android:textSize="@dimen/textSizeTitle">
        </TextView>
    </LinearLayout>
    <!--Relay-->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="10dp"
        android:gravity="center">
        <Button
            style="@style/ButtonRelayTheme"
            android:id="@+id/btnRelayON"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="15dp"
            app:icon="@drawable/ic_button_power"
            android:text="@string/onText" />
        <Button
            style="@style/ButtonRelayTheme"
            android:id="@+id/btnRelayOFF"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@+id/btnRelayON"
            android:layout_marginStart="10dp"
            android:padding="15dp"
            app:icon="@drawable/ic_button_power"
            android:text="@string/offText" />
    </RelativeLayout>
</RelativeLayout>